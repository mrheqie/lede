name: Build JCG Q30 PRO Firmware

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Environment
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential clang flex bison g++ gawk gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev python3-distutils rsync unzip zlib1g-dev

      - name: Prepare Source
        run: |
          git config --global credential.helper ""
          git clone -b master https://github.com/immortalwrt/immortalwrt openwrt
          cd openwrt
          
          # 1. 仅添加一次 Passwall 源，因为它包含所有内容
          echo "src-git passwall https://github.com/Openwrt-Passwall/openwrt-passwall.git;main" >> feeds.conf.default
          
          # 2. 添加 EasyTier
          echo "src-git easytier https://github.com/EasyTier/luci-app-easytier.git" >> feeds.conf.default
          
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Verify Feeds Installation
        run: |
          echo "=== 开始验证 Passwall ==="
          if [ -n "$(find openwrt/ -name "luci-app-passwall" -type d)" ]; then
            echo "✅ Passwall LuCI 已找到"
          else
            echo "❌ 严重错误：未找到 Passwall 模块！"
            find openwrt/ -name "luci-app-passwall"
            exit 1
          fi
          
          echo "=== 开始验证 EasyTier ==="
          if [ -n "$(find openwrt/ -name "luci-app-easytier" -type d)" ]; then
            echo "✅ EasyTier 已找到"
          else
            echo "❌ 严重错误：未找到 EasyTier 模块！"
            exit 1
          fi

      - name: Cache Downloads
        uses: actions/cache@v4
        with:
          # 仅缓存 dl 目录 (源码包)，不要缓存 build_dir，因为 Github Actions 缓存限制 10GB 容易爆，且跨版本容易报错
          path: openwrt/dl
          key: imm-jcg-q30-ubootmod-dl-${{ hashFiles('.github/workflows/*.yml') }}
          restore-keys: |
            imm-jcg-q30-ubootmod-dl-

      - name: Configure Firmware
        run: |
          cd openwrt
          
          # 清理旧配置确保纯净
          rm -f .config
          
          # 1. 基础架构设定
          echo "CONFIG_TARGET_mediatek=y" >> .config
          echo "CONFIG_TARGET_mediatek_filogic=y" >> .config
          
          # 2. 【核心修复】开启多设备编译支持（防止被默认配置强制覆盖）
          echo "CONFIG_TARGET_MULTI_PROFILE=y" >> .config
          
          # 3. 【核心修复】明确选中 JCG Q30 PRO (这里必须用下划线)
          echo "CONFIG_TARGET_DEVICE_mediatek_filogic_DEVICE_jcg_q30_pro=y" >> .config
          
          # 4. 【核心修复】显式禁用默认的 openwrt_one
          echo "# CONFIG_TARGET_DEVICE_mediatek_filogic_DEVICE_openwrt_one is not set" >> .config
          
          # 5. 核心功能与补丁
          echo "CONFIG_PACKAGE_luci-app-passwall=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Xray_Plugin=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-passwall_INCLUDE_SingBox=y" >> .config
          echo "CONFIG_PACKAGE_geoview=y" >> .config 
          echo "CONFIG_PACKAGE_luci-app-easytier=y" >> .config
          echo "CONFIG_PACKAGE_kmod-tun=y" >> .config
          echo "CONFIG_PACKAGE_kmod-nft-fullcone=y" >> .config
          
          # 生成初步依赖树
          make defconfig
          
          # 6. 深度精简 (前后端包一网打尽)
          PLUGINS_TO_REMOVE=(
            "luci-app-aria2" "aria2" "luci-app-transmission" "transmission-daemon" "transmission-web"
            "luci-app-qbittorrent" "qbittorrent" "luci-app-samba4" "samba4-server" "samba4-libs"
            "luci-app-minidlna" "minidlna" "luci-app-filemanager" "dufs"
            "luci-app-dae" "dae" "luci-app-openclash" "luci-app-softether" 
            "luci-app-tailscale" "tailscale"
          )
          
          for pkg in "${PLUGINS_TO_REMOVE[@]}"; do
            sed -i "/CONFIG_PACKAGE_${pkg}=y/d" .config
            echo "CONFIG_PACKAGE_${pkg}=n" >> .config
          done
          
          # 最终生成配置
          make defconfig
          
          # 验证精简生效
          for pkg in "${PLUGINS_TO_REMOVE[@]}"; do
            if grep -q "CONFIG_PACKAGE_${pkg}=y" .config; then
              echo "❌ 警告: ${pkg} 强制启用了依赖，请手动排查"
              exit 1
            fi
          done

      - name: Verify Configuration (Fail-Fast)
        run: |
          cd openwrt
          echo "=== 正在进行编译前终极校验 ==="
          
          if ! grep -q "CONFIG_TARGET_DEVICE_mediatek_filogic_DEVICE_jcg_q30_pro=y" .config; then
            echo "❌ 致命错误：.config 中未检测到 JCG Q30 PRO 的激活状态！"
            echo "当前文件里实际被选中的设备是："
            grep "CONFIG_TARGET_DEVICE_.*=y" .config || echo "(没有任何设备被选中)"
            exit 1
          fi
          
          if grep -q "CONFIG_TARGET_DEVICE_mediatek_filogic_DEVICE_openwrt_one=y" .config; then
            echo "❌ 致命错误：openwrt_one 居然还活着！这会导致编译时间翻倍！"
            exit 1
          fi
          
          echo "✅ 校验完美通过！JCG Q30 PRO 专属配置已锁定，准备进入 3 小时熬鹰模式！🚀"

      - name: Download package sources
        run: |
          cd openwrt
          # 提前下载所有依赖，防止多线程编译时因网络问题崩溃
          make download -j8 || make download -j1 V=s

      - name: Compile Firmware
        run: |
          cd openwrt
          # 使用 nproc 自动获取最佳线程数，如果失败则回退到单线程详细输出报错
          make -j$(nproc) || make -j1 V=s

      - name: Post-Build Verify & Prepare Upload
        run: |
          cd openwrt/bin/targets/mediatek/filogic
          
          echo "=== 当前目录所有文件 ==="
          ls -lah
          
          # 抓取包含 jcg_q30 的正确固件（这里匹配的是 filogic.mk 里的横杠命名）
          FIRMWARE=$(ls -1 *jcg_q30*squashfs-sysupgrade.itb 2>/dev/null | head -n1)
          
          if [ -z "$FIRMWARE" ]; then
            echo "❌ 致命错误: 未找到 JCG Q30 PRO 专属固件!"
            exit 1
          fi
          
          SIZE=$(stat -c%s "$FIRMWARE")
          echo "✅ 找到固件: $FIRMWARE (大小: $((SIZE/1024/1024)) MB)"
          
          if [ "$SIZE" -lt 10485760 ]; then
            echo "❌ 致命错误: 固件大小小于 10MB，这不是完整固件!"
            exit 1
          fi
          
          # 生成校验和
          sha256sum *jcg_q30*.itb > sha256sums.txt
          echo "✅ 固件校验通过，准备上传"

      - name: Upload Firmware
        uses: actions/upload-artifact@v4
        with:
          name: openwrt-jcg-q30-pro-ubootmod
          path: |
            openwrt/bin/targets/mediatek/filogic/*-squashfs-sysupgrade.itb
            openwrt/bin/targets/mediatek/filogic/sha256sums.txt
          retention-days: 7
