name: Build Kunpeng CC (MT7628 + EC200A) sing-box Firmware

on:
  repository_dispatch:
  workflow_dispatch:

env:
  REPO_URL: https://github.com/coolsnowwolf/lede
  REPO_BRANCH: master
  TZ: Asia/Shanghai
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Initialization environment
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/powershell /usr/share/swift /var/lib/apt/lists/*
        sudo rm -rf /usr/local/share/boost /opt/az /usr/share/game /usr/local/lib/node_modules
        sudo -E apt-get -qq update
        sudo -E apt-get -qq install -y --no-install-recommends ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential bzip2 ccache cmake xxd cpio curl device-tree-compiler fastjar flex gawk gettext gcc-multilib g++-multilib git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev libreadline-dev libssl-dev libtool lrzsz mkisofs msmtp ninja-build p7zip p7zip-full patch pkgconf python3 python3-pyelftools python3-setuptools qemu-utils rsync scons squashfs-tools subversion swig texinfo u-boot-tools upx-ucl unzip xmlto xz-utils zlib1g-dev
        sudo -E apt-get -qq autoremove --purge
        sudo -E apt-get -qq clean
        sudo timedatectl set-timezone "$TZ"
        sudo mkdir -p /workdir
        sudo chown $USER:$USER /workdir

    - name: Clone source code
      working-directory: /workdir
      run: |
        git clone $REPO_URL -b $REPO_BRANCH lede
        ln -sf /workdir/lede $GITHUB_WORKSPACE/lede

    - name: Update & Install feeds
      run: |
        cd lede
        ./scripts/feeds update -a
        ./scripts/feeds install -a
        # 强行拉取 Homeproxy
        git clone --depth=1 https://github.com/immortalwrt/homeproxy.git package/homeproxy

    - name: Generate Custom Configuration (.config)
      run: |
        cd lede
        
        # 1. 核心架构与机型
        echo "CONFIG_TARGET_ramips=y" > .config
        echo "CONFIG_TARGET_ramips_mt76x8=y" >> .config
        echo "CONFIG_TARGET_ramips_mt76x8_DEVICE_nradio_wt6285=y" >> .config
        
        # 2. 基础网页与核心硬件加速
        echo "CONFIG_PACKAGE_luci=y" >> .config
        echo "CONFIG_LUCI_LANG_zh_Hans=y" >> .config
        echo "CONFIG_PACKAGE_luci-theme-bootstrap=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-firewall=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-turboacc=y" >> .config
        echo "CONFIG_PACKAGE_luci-compat=y" >> .config
        
        # 3. 【代理核心】启用 Homeproxy 与 Sing-box
        echo "CONFIG_PACKAGE_luci-app-homeproxy=y" >> .config
        echo "CONFIG_PACKAGE_sing-box=y" >> .config
        echo "CONFIG_PACKAGE_upx=y" >> .config
        echo "CONFIG_PACKAGE_curl=y" >> .config
        echo "CONFIG_PACKAGE_ca-bundle=y" >> .config
        
        # 彻底切断旧时代协议依赖
        echo "# CONFIG_PACKAGE_luci-app-ssr-plus is not set" >> .config
        echo "# CONFIG_PACKAGE_luci-app-passwall is not set" >> .config
        echo "# CONFIG_PACKAGE_luci-app-openclash is not set" >> .config
        
        # 4. 【4G 驱动与核心工具】保留 QMI
        echo "CONFIG_PACKAGE_kmod-usb-core=y" >> .config
        echo "CONFIG_PACKAGE_kmod-usb2=y" >> .config
        echo "CONFIG_PACKAGE_kmod-usb-net=y" >> .config
        echo "CONFIG_PACKAGE_kmod-usb-net-qmi-wwan=y" >> .config
        echo "CONFIG_PACKAGE_kmod-usb-serial=y" >> .config
        echo "CONFIG_PACKAGE_kmod-usb-serial-wwan=y" >> .config
        echo "CONFIG_PACKAGE_kmod-usb-serial-option=y" >> .config
        echo "CONFIG_PACKAGE_usb-modeswitch=y" >> .config
        echo "CONFIG_PACKAGE_luci-proto-3g=y" >> .config
        echo "CONFIG_PACKAGE_luci-proto-qmi=y" >> .config
        echo "CONFIG_PACKAGE_comgt=y" >> .config
        echo "CONFIG_PACKAGE_uqmi=y" >> .config
        
        # 砍掉网卡模拟冗余
        echo "# CONFIG_PACKAGE_kmod-usb-net-cdc-ether is not set" >> .config
        echo "# CONFIG_PACKAGE_kmod-usb-net-rndis is not set" >> .config
        echo "# CONFIG_PACKAGE_kmod-usb-net-cdc-ncm is not set" >> .config
        echo "# CONFIG_PACKAGE_usb-modem-node is not set" >> .config
        
        # 5. 【极限杀后台】
        echo "# CONFIG_PACKAGE_nlbwmon is not set" >> .config
        echo "# CONFIG_PACKAGE_tethering is not set" >> .config
        echo "# CONFIG_PACKAGE_luci-app-vlmcsd is not set" >> .config
        echo "# CONFIG_PACKAGE_vlmcsd is not set" >> .config
        echo "# CONFIG_PACKAGE_luci-app-vsftpd is not set" >> .config
        echo "# CONFIG_PACKAGE_vsftpd-alt is not set" >> .config
        echo "# CONFIG_PACKAGE_luci-app-wol is not set" >> .config
        echo "# CONFIG_PACKAGE_luci-app-upnp is not set" >> .config
        echo "# CONFIG_PACKAGE_miniupnpd is not set" >> .config
        echo "# CONFIG_PACKAGE_luci-app-ddns is not set" >> .config
        echo "# CONFIG_PACKAGE_luci-app-arpbind is not set" >> .config
        echo "# CONFIG_PACKAGE_luci-app-accesscontrol is not set" >> .config
        echo "# CONFIG_PACKAGE_luci-app-rclone is not set" >> .config
        echo "# CONFIG_PACKAGE_luci-app-uugamebooster is not set" >> .config
        echo "# CONFIG_PACKAGE_luci-app-xlnetacc is not set" >> .config
        echo "# CONFIG_PACKAGE_luci-app-sqm is not set" >> .config
        echo "# CONFIG_PACKAGE_luci-mod-status is not set" >> .config
        echo "# CONFIG_PACKAGE_collectd is not set" >> .config
        
        # 6. 魔改 Sing-box Makefile (★ 精准切除 + PostPrepare 删除 tailscale)
        echo "=== 正在执行 Sing-box 条件片段精准切除 ==="
        
        if [ -f "package/homeproxy/files/usr/bin/sing-box" ] || [ -f "package/homeproxy/root/usr/bin/sing-box" ]; then
            echo "❌ 致命拦截：检测到预置二进制"
            exit 1
        fi
        
        SING_MAKEFILES=$(find ./feeds ./package -type f -path "*/sing-box/Makefile")
        if [ -z "$SING_MAKEFILES" ]; then
            echo "❌ 未找到 sing-box Makefile"
            exit 1
        fi
        
        for mk in $SING_MAKEFILES; do
            echo "-> 正在手术切除 $mk 中的巨型组件条件并设置 tailscale 删除钩子"
            
            # 1. 删除所有 $(if $(CONFIG_...with_gvisor)...) 条件片段
            sed -i 's/\$(if \$(CONFIG_SING_BOX_BUILD_GVISOR),with_gvisor)//g' "$mk"
            sed -i 's/\$(if \$(CONFIG_SING_BOX_BUILD_QUIC),with_quic)//g' "$mk"
            sed -i 's/\$(if \$(CONFIG_SING_BOX_BUILD_V2RAY_API),with_v2ray_api)//g' "$mk"
            sed -i 's/\$(if \$(CONFIG_SING_BOX_BUILD_WIREGUARD),with_wireguard)//g' "$mk"
            
            # 2. 注释掉包含禁用标签关键词的整行（防止硬编码）
            sed -i '/with_gvisor\|with_quic\|with_v2ray_api\|with_wireguard/s/^/#/' "$mk"
            
            # 3. 追加 Build/PostPrepare 钩子，在编译前删除 tailscale 源码（根治编译错误）
            cat >> "$mk" <<'EOF'
        
        define Build/PostPrepare
        	rm -rf $(PKG_BUILD_DIR)/protocol/tailscale
        endef
        EOF
            
            # 4. 在文件末尾强制追加最终极简标签（override 确保覆盖所有定义）
            echo "override GO_PKG_TAGS:=with_utls,with_anytls,with_shadowsocks" >> "$mk"
            echo "✅ 手术成功，已注入 override 标签并设置 tailscale 删除钩子"
        done
        
        # 最终验证：检查非注释行中是否还有禁用标签
        if grep -v "^#" $SING_MAKEFILES | grep -qE "with_gvisor|with_wireguard|with_quic|with_v2ray"; then
            echo "❌ 严重警告：非注释行中仍有巨型组件！"
            grep -v "^#" $SING_MAKEFILES | grep -nE "with_gvisor|with_wireguard|with_quic|with_v2ray"
            exit 1
        fi
        
        if ! grep -v "^#" $SING_MAKEFILES | grep -q "with_anytls" || ! grep -v "^#" $SING_MAKEFILES | grep -q "with_shadowsocks"; then
            echo "❌ 致命错误：AnyTLS 或 Shadowsocks 协议标签丢失！"
            exit 1
        fi
        
        echo "✅ 协议集成验证通过，Sing-box 净化成功。"
        
        # 7. 生成最终依赖
        make defconfig

    - name: Download package
      id: package
      run: |
        cd lede
        make download -j8
        find dl -size -1024c -exec ls -l {} \;
        find dl -size -1024c -exec rm -f {} \;

    - name: Compile the firmware
      id: compile
      run: |
        cd lede
        make -j$(nproc) || make -j1 V=s
        echo "status=success" >> $GITHUB_OUTPUT

    # 安全探针：体积超限直接熔断
    - name: Check Firmware Size (Safety Guard)
      if: steps.compile.outputs.status == 'success'
      run: |
        cd lede/bin/targets/ramips/mt76x8/
        echo "=== 固件体积报告 ==="
        ls -lh *sysupgrade*.bin
        FILE=$(ls *sysupgrade*.bin | head -n 1)
        SIZE=$(stat -c%s "$FILE")
        if [ "$SIZE" -gt 16000000 ]; then
          echo "❌ 致命错误：固件体积高达 $SIZE 字节，超过 16MB 物理限制，强行刷入会导致路由器变砖！"
          exit 1
        else
          echo "✅ 固件体积安全 ($SIZE 字节)，可以放心刷入！"
        fi

    - name: Upload firmware to Artifacts
      uses: actions/upload-artifact@v4
      if: success()
      with:
        name: OpenWrt_KunpengCC_EC200A_Homeproxy_AnyTLS
        path: lede/bin/targets/ramips/mt76x8/*sysupgrade*.bin
        retention-days: 7
